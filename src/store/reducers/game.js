const initialState = {
  userId: null,
  categoryId: null,
  words: [],
  word: {}, // { value: '', weight: 5 }
  letter: "",
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
